@model AdsGridPartialViewModel
@inject IViewLocalizer Localizer

<link rel="stylesheet" href="~/css/AdsGridPartialView.css" type="text/css" />

<p class="ads-partial-view-title">@Localizer["Ads"]</p>

<div class="container">

    @for (int i = 0; i < 3; i++)
    {
        <div class="row">

            @for (int e = 0; e < 4; e++)
            {
                var base64 = Convert.ToBase64String(Model.Picture);
                var imgSrc = String.Format("data:image/gif;base64,{0}", @base64);

                <div id="ad" class="col-lg-3 col-md-3">

                    <div>
                        <img alt="Picture" src="@imgSrc" class="ad-img" /><br />
                        <span class="ad-title">@Model.AdTitle</span>
                        <br />
                        <span class="ad-publication-date">@Model.PublicationDate.ToShortDateString()</span>
                        <a class="ad-link" asp-action="Ad" asp-controller="Home" asp-area="Ads" asp-id="@Model.AdId">@Localizer["Review"]</a>
                    </div>

                </div>
            }
        </div>
    }
</div>